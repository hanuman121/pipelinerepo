pipeline {
   agent any

   stages {
      stage('Running Job3') {
         steps {
            echo 'job3'
         }
      }

      stage('Running Job2') {
         steps {
            echo 'job2'
         }
      }
      stage('Running Job1') {
         steps {
            echo 'job1'
         }
      }
   }
